import { createStitches } from "@stitches/react";

export const { styled } = createStitches({
  theme: {
    colors: {
      // PALETTE
      // ASSIGNMENT
    },
    fontSizes: {},
    lineHeights: {},
    fontWeights: {},
  },
  media: {},
});
